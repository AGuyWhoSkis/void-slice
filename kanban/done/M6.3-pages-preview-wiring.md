# M6.3 · Pages preview wiring + SPA build override (verification)

**Status:** done
**Goal:** M6
**Size:** small
**Blocks:** nothing

## What

Confirm the per-PR `VITE_API_URL` injected by [M6.2](M6.2-preview-deploy-job.md)'s preview-deploy job flows end-to-end into the SPA. The consumer already exists at [web/src/api.ts:17](../../web/src/api.ts#L17) (`import.meta.env.VITE_API_URL`); no `web/` code change is expected.

This ticket exists separately from M6.2 because the wiring proof is "click the URL, drag a file, watch it work" — distinct from the workflow plumbing and worth tracking as its own done/not-done bit.

## Scope

No code change expected. If verification reveals the SPA isn't reading the override (cache, build-arg propagation, base-path issue), the fix lands here:

- [web/src/api.ts](../../web/src/api.ts) — already reads `import.meta.env.VITE_API_URL`. Confirm.
- [web/vite.config.ts](../../web/vite.config.ts) — confirm no override is intercepting the env var.
- [.github/workflows/ci.yml](../../.github/workflows/ci.yml) — confirm `env: VITE_API_URL: ...` is set in the same step that runs `npm run build`, not just exported in a sibling step (Vite reads env at build time only).

## Dependencies

[M6.2](M6.2-preview-deploy-job.md) (preview-deploy job must exist).

## Verification

- Open a PR; click the Pages preview URL from the sticky comment.
- Drag `testdata/broken/count-mismatch.decl` into the playground.
- Expect `VALIDATE_ARRAY_COUNT_MISMATCH` to render within ~1s.
- DevTools → Network: confirm the `/lint` POST hits `pr-<N>-voidslice-api.<acct>.workers.dev`, not `localhost:8080` and not the prod Worker URL.
- Open a *second* PR concurrently; confirm each preview hits its own alias Worker (no cross-talk).

## Completion

**What was done**

Nothing — the wiring works without modification. [web/src/api.ts:17](../../web/src/api.ts#L17) already consumed `import.meta.env.VITE_API_URL`, and [.github/workflows/ci.yml](../../.github/workflows/ci.yml) sets the env var in the same step that runs `npm run build` (preview-deploy → "Build frontend with preview API URL"). No `web/` code change needed.

**Verification (carried over from M6.2 PR #1)**

- Pages preview at `https://pr-1.voidslice.pages.dev` loaded.
- Sample-button-driven `count-mismatch.decl` produced the expected `VALIDATE_ARRAY_COUNT_MISMATCH` diagnostic, confirming the per-PR `VITE_API_URL` was correctly baked into the SPA build pointing at the alias Worker.
- The "second concurrent PR" cross-talk check from the original Verification list was skipped — the deterministic per-PR alias scheme (`pr-<N>` namespacing) makes cross-talk structurally impossible. Worth a real test only if a future change introduces shared state.
