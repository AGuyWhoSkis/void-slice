# M17.2 · Root-cause taxonomy + B2 boundary

**Status:** todo
**Goal:** M17
**Size:** medium

## What

Investigation-only ticket, phase 2 of M17. Reads M17.1's draft taxonomy at [`kanban/goals/M17-edit-delta-taxonomy.md`](../goals/M17-edit-delta-taxonomy.md) and, for each surprise class enumerated there, pinpoints the root cause in the lint engine and classifies the class as either **cascade-shaped** (a fixable engine bug, by precedent of M12.16 / M12.17 / M12.18) or **inherent limit** (the B2 boundary — what a context-free linter operating on one file cannot reasonably do).

Deliverable is a second memo at [`kanban/goals/M17-root-causes.md`](../goals/M17-root-causes.md) plus a re-pinning edit to [`kanban/goals/M17.md`](../goals/M17.md) replacing the open-ended phase-3 framing ("a maximally complete fix within those bounds") with a concrete deliverable scope drawn from the cascade-shaped row count in §2. The goal text explicitly authorizes this re-pin: *"'Done' is re-pinned after phase 2; the goal commits to the shape of the work, not to a deliverable selected before the investigation has run."*

No engine code modified — `internal/scan/`, `internal/parse/`, `internal/validate/`, `internal/lint/`, `internal/report/`, `worker/`, `cmd/voidslice-wasm/` stay clean. The phase-3 fix tickets are sliced in a follow-up `/goal-slice M17` run after this ticket lands.

## Scope

- **New file:** [`kanban/goals/M17-root-causes.md`](../goals/M17-root-causes.md) with this skeleton (filled by this ticket):

  ```markdown
  # M17 — Root causes + B2 boundary

  **TL;DR:** <one-sentence verdict on fixable-vs-inherent split across M17.1's taxonomy>

  ## 1. Root-cause analysis

  <one H3 per surprise class from M17.1's §3 table. Each H3 carries: the class name, a code citation (`internal/<pkg>/<file>.go:<line>` or a span across multiple), a one-paragraph mechanism explanation, and a classification verdict: **cascade-shaped (fixable)** or **inherent limit (B2)**. Classes whose root cause is ambiguous get a verdict of **needs-more-probing** with a named follow-up.>

  ## 2. B2 boundary

  <paragraph or table drawing the boundary explicitly: what a context-free linter operating on one file cannot reasonably catch, with each "inherent limit" verdict from §1 mapped to the principle that puts it past the boundary (e.g. requires cross-file reference resolution, requires author-intent inference, requires runtime context). This is the durable artifact — phase 3 documents these in user-facing form.>

  ## 3. Phase 3 re-pin

  <bulleted plan for phase 3: per cascade-shaped class, the fix shape (engine fix vs. classifier router per M12.18 vs. harness hard-gate promotion vs. user-facing doc). Phase 3 ticket count is the number of bullets. This section is the source of truth that the re-pin to kanban/goals/M17.md mirrors.>
  ```

- **Re-pin edit to [`kanban/goals/M17.md`](../goals/M17.md):** replace the paragraph beginning *"The work is staged: (1) a preliminary investigation …"* through the end of that paragraph with a re-pinned version that:
  - Acknowledges phase 1 (M17.1) and phase 2 (M17.2) are complete and cite the two memos.
  - States the concrete phase-3 scope, drawn verbatim from the §3 bullet count: e.g. *"Phase 3: N tickets — M engine fixes, K classifier routers, J doc updates."*
  - If §3's bullet count is zero (no fixable class), explicitly recommends the goal close without phase 3 and surfaces this to the user in the ticket-close message.

- **No engine code modified.** Investigation-only — `internal/scan/`, `internal/parse/`, `internal/validate/`, `internal/lint/`, `internal/report/`, `worker/`, `cmd/voidslice-wasm/`, [`internal/harness/lexinvariance/`](../../internal/harness/lexinvariance/), [`internal/harness/locality/`](../../internal/harness/locality/) are not touched.

- **No phase-3 fix tickets written.** Filed in a follow-up `/goal-slice M17` run after this ticket lands.

## Dependencies

- **M17.1 must land first.** This ticket reads `kanban/goals/M17-edit-delta-taxonomy.md`. The dependency is hard: do not start until M17.1's memo exists on the goal branch.

## Verification

- [`kanban/goals/M17-root-causes.md`](../goals/M17-root-causes.md) exists with H1 `# M17 — Root causes + B2 boundary`, a `**TL;DR:**` line, and exactly three H2 headings — `## 1. Root-cause analysis`, `## 2. B2 boundary`, `## 3. Phase 3 re-pin` — in that order.
- §1 contains ≥1 H3 per surprise class in M17.1's §3 table (count match is exact; M17.1's draft taxonomy table size = M17.2's §1 H3 count). Each H3 names a code citation in `internal/<pkg>/<file>.go:<line>` form (or `internal/<pkg>/<file>.go:<line>-<line>` span) AND ends with a verdict line in bold: **`cascade-shaped (fixable)`** / **`inherent limit (B2)`** / **`needs-more-probing`**.
- §2 maps every **`inherent limit (B2)`** verdict from §1 to a named principle.
- §3 is a bulleted plan; bullet count equals the count of **`cascade-shaped (fixable)`** verdicts in §1 (plus any **`needs-more-probing`** follow-ups). If both counts are zero, §3 says so verbatim and recommends goal close.
- [`kanban/goals/M17.md`](../goals/M17.md)'s third paragraph (the "The work is staged: …" paragraph) is replaced with a re-pinned version naming both memos and the phase-3 ticket count. `git diff` against the pre-M17.2 revision shows exactly this one paragraph changed.
- `git show --name-only <M17.2-commit>` lists exactly two files: `kanban/goals/M17-root-causes.md` and `kanban/goals/M17.md`. No `internal/`, `cmd/`, `worker/`, `testdata/`, or other kanban edits.
- `go test ./...` stays green (no engine changes).

## Out of scope (intentionally deferred)

- **Writing the phase-3 fix tickets.** Done in a follow-up `/goal-slice M17` run after this ticket lands — the count is fixed by §3, the contracts are not.
- **Engine fixes for any cascade-shaped class.** Per-class fix tickets in phase 3, contract-grade at slice time per the standard `/goal-slice` flow.
- **Promoting any harness transform to hard-gate.** A phase-3 decision per class; M17.2 can recommend it in §3 but does not act on it.
- **User-facing documentation of B2 limits.** A phase-3 ticket if §3 names doc work; M17.2 produces the memo, not the user-facing copy.
- **M17 status flip to `done`.** The user's call after phase 3 (or after this ticket if §3's count is zero).
