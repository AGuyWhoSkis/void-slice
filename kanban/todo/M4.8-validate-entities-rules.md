# M4.8 · Validate-rule refinement on existing `.entities`

**Status:** todo
**Goal:** M4
**Size:** small

## What

Phase C of the goal: with parse no longer drowning the signal, the validate-package rules can be re-examined against real corpus output. Today, the existing `.entities` file produces `VALIDATE_ARRAY_COUNT_MISMATCH ×7` and `VALIDATE_ARRAY_MISSING_NUM ×1` — the only validate diagnostics in the corpus. The taxonomy doc lists them as "separate Phase C work." This ticket investigates each instance, then either fixes the false positive in the rule or retires the rule if it can't be justified against real game files. `LINT_VE_INCONSISTENCY ×1` is correct behavior and stays untouched.

This is the **validator-layer golden coverage** ticket.

## Scope

- Audit each of the 8 validate-warning instances on the existing `.entities` corpus file. For each: confirm whether the warning reflects a real defect in the file, a defect in the rule, or a defect in our model of the file format.
- For false-positive instances: refine the rule (preferred) or retire it (fall-back, if the rule has no plausible use against real game files).
- For real-defect instances: keep the rule, document why the file legitimately triggers it (the file may be a known-bad fixture; if so, move it to a negative-test path), and re-baseline M4.2's allowlist with justification.
- `LINT_VE_INCONSISTENCY` is left untouched — it is correct.

## Dependencies

None (decoupled from grammar work; can run in parallel with M4.4–M4.7).

## Verification

- New validate-package golden test pinning the validator's diagnostic output on the existing `.entities` representative — asserts the post-refinement diagnostic set, whatever it ends up being. **Validator-layer golden coverage.**
- M4.2's harness `VALIDATE_ARRAY_COUNT_MISMATCH` (7) + `VALIDATE_ARRAY_MISSING_NUM` (1) rows resolve: drained to zero, or kept at a re-baselined number with the justification recorded.
- `go test ./...` green.
