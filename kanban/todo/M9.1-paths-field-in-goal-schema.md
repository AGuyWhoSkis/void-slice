# M9.1 · `Paths:` field in goal schema

**Status:** todo
**Goal:** M9
**Size:** small

## What

Add a machine-readable `**Paths:**` field to the goal file format so downstream tooling (overlap surfacing in `/goal-define`, refusal in `/goal-dispatch`) can compare goals' surfaces without parsing prose. M9 is the only currently active goal that needs to declare `Paths:`; closed goals stay bare.

## Scope

- `**Paths:**` field documented in `kanban/README.md` alongside the existing goal schema description
- Format chosen and documented (e.g. comma-separated globs, one path per line, etc. — decided at implementation time)
- M9's own goal file declares its `Paths:`
- Closed goals (M1–M8) remain unchanged — overlap checking only matters for in-flight goals

## Dependencies

None.

## Verification

`kanban/goals/M9.md` has a `**Paths:**` field consistent with the format documented in `kanban/README.md`. No other goal files are modified.
