# M4.2 · Allowlist-driven golden corpus harness

**Status:** todo
**Goal:** M4
**Size:** small

## What

The goal's binary success metric is "zero un-allowlisted diagnostics on `testdata/golden/`." Today's [internal/lint/clean_sweep_test.go](../../internal/lint/clean_sweep_test.go) walks the corpus but logs known false positives via `t.Logf` rather than failing — so the test stays green even as counts drift. This ticket converts that walker into a strict allowlist-driven harness seeded from the inventory in [M4-decl-taxonomy.md § Allowlist inventory for M4.2](../goals/M4-decl-taxonomy.md). New unallowlisted codes fail the build; allowlisted-code count growth fails the build. The harness starts red against today's 754,265 hits and drains as M4.3–M4.8 land. M4 closes (M4.11) when the harness is green with only the documented permanent residual.

This is the **lint-facade golden coverage** for M4.

## Scope

- `TestCleanSweep` (or successor) walks `testdata/golden/` recursively (flat root + `d2/game1/` + `doto/game1/`) and asserts diagnostics against a structured allowlist: per-`{code, file-or-shape}` count, with strict equality (any change — up or down — fails until intentionally re-baselined).
- Allowlist seeded verbatim from the taxonomy doc's inventory table (Shape-4 PARSE/VOID permanent; Shapes 1/2/3/5 + Sidecar PARSE/VOID drain over M4.3–M4.7; entities-validate-warns drain over M4.8).
- `LINT_VE_INCONSISTENCY` (×1) stays *out* of the allowlist — it is correct behavior.
- Failure messages name the file path and the count delta so the ticket draining the row can re-baseline confidently.

## Dependencies

None. Lands first so subsequent grammar tickets have a green-bar contract to drain against.

## Verification

- `go test ./internal/lint/...` runs the harness; it fails today (matching the inventory) and passes once a re-baseline matches the new state. Green is the M4-close signal.
- The harness is the lint-layer golden test; M4.11 confirms it runs green without manual allowlist edits beyond the documented permanent residual.
