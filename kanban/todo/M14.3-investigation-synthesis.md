# M14.3 · Investigation synthesis

**Status:** todo
**Goal:** M14
**Size:** small

## What

Close the spike by writing the four pieces that wrap §1–§3 of [`kanban/goals/M14-declared-typing-investigation.md`](../../kanban/goals/M14-declared-typing-investigation.md) into a publishable investigation memo in the [M12-cascade-investigation](../../kanban/goals/M12-cascade-investigation.md) shape:

1. **TL;DR** — one or two sentences capturing the verdicts of audits A, B, and C, plus whether a diagnostic shipped.
2. **Methodology footnote** — a short paragraph in the footer block citing the throwaway scripts at `/tmp/m14-audit/` and `/tmp/m14-audit-c/`, naming what each did, and stating that they are not committed (per M12.1 precedent).
3. **gameN-bucket gap statement** — a paragraph naming the corpus gap (only `d2/game1` and `doto/game1` are present) and stating, as an *acquisition question*, what fixtures would have to be added for a follow-up goal to widen coverage. No commitment to acquire — just the precise gap.
4. **Named next-goal proposal** — at the bottom of the report, propose the next concrete goal from a position of evidence. The proposal must be a named candidate (e.g. "M15: ship audit-C diagnostic across N kinds" / "M15: build a `.decl.xml`-backed resolver for kind X" / "M15: acquire gameN fixtures and re-run audits A and B"), with a one-paragraph justification grounded in §1–§3's measured facts, **not** in the G-A / G-B framings the spike was meant to retire.

No new measurements. No new code. No new fixtures. M14.3 is an authorial pass over content M14.1 and M14.2 produced.

## Scope

- **Edit only:** `kanban/goals/M14-declared-typing-investigation.md`. Fill the `**TL;DR:**` line, the footer block (which currently reads `_<footer filled by M14.3: methodology footnote, gameN-bucket gap, named next-goal proposal>_`), and append the next-goal proposal as a final H2 section `## Next goal` below the footer's horizontal rule.

- **TL;DR shape.** One sentence covering audits A and B's headline rates; one sentence covering audit C's verdict (rule picked or none qualified; shipped or not). Mirrors M12-cascade-investigation's TL;DR density.

- **Methodology footnote shape.** Replace the footer stub with prose along the lines of:

  ```markdown
  ---

  <a id="methodology"></a>
  **Methodology footnote.** Audits A and B were run by `/tmp/m14-audit/` (~<N> lines of Go, not committed): <one-sentence description>. Audit C's rule-survey and FP measurement were run by `/tmp/m14-audit-c/` (~<N> lines of Go, not committed): <one-sentence description>. Both are reproducible on a dev machine with the golden corpus in place.

  **gameN-bucket gap.** The corpus under `testdata/golden/` covers `d2/game1` and `doto/game1` only. Extending audits A and B to game2+ would require acquiring <concrete asset description>; the spike does not commit to that acquisition — it states the gap so a follow-up goal can scope it deliberately.
  ```

- **Next-goal proposal shape.** A new H2 `## Next goal` after the footer, naming one candidate with a paragraph of evidence-grounded justification. Must explicitly retire the parked G-A / G-B framings cited in [`kanban/goals/M14.md`](../../kanban/goals/M14.md) — i.e. state that those framings are superseded by this proposal — rather than picking one of them by name.

- **No edits to §1, §2, §3.** Those are M14.1's and M14.2's contracts; M14.3 reads them and synthesizes.

- **No edit to `kanban/goals/M14.md`.** Goal-status flip is the user's call after the PR lands.

- **No new files.** `kanban/goals/M14-declared-typing-investigation.md` is the only file touched.

## Dependencies

M14.1 and M14.2 — both §1/§2 and §3 must be non-stub before the synthesis pass.

## Verification

- `kanban/goals/M14-declared-typing-investigation.md`'s `**TL;DR:**` line is two sentences or fewer, names audits A/B/C by letter, and states whether a diagnostic shipped.
- Footer block contains a `**Methodology footnote.**` paragraph naming both `/tmp/m14-audit/` and `/tmp/m14-audit-c/` and stating they're not committed.
- Footer block contains a `**gameN-bucket gap.**` paragraph naming the corpus coverage (`d2/game1`, `doto/game1`) and the concrete acquisition that would be required.
- File ends with an H2 `## Next goal` section naming one candidate and a paragraph that explicitly notes the G-A / G-B candidate-goal framings are retired by this proposal.
- `grep -c '_<filled by M14\.' kanban/goals/M14-declared-typing-investigation.md` returns `0` (all stubs filled).
- `git show --name-only <M14.3-commit>` lists only `kanban/goals/M14-declared-typing-investigation.md`. No `internal/`, `cmd/`, `worker/`, `testdata/`, or other kanban edits.

## Out of scope (intentionally deferred)

- **Re-running or extending audits A, B, C.** New evidence is M14.1/M14.2's territory; M14.3 only synthesizes what's already on the page.
- **Filing the next-goal as a new `kanban/goals/M15.md`.** The proposal is a paragraph in this report; defining it as a goal is a `/goal-define` step the user takes after the spike PR lands.
- **Closing M14.md (status flip to done).** User's call.
- **Acquiring gameN fixtures.** Stated as a gap; acquisition belongs to the next goal.
