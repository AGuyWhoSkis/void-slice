# M4.12 · diagdump errcheck cleanup

**Status:** todo
**Goal:** M4
**Size:** xs
**Origin:** M4.8

## What

[cmd/diagdump/main.go:16](../../cmd/diagdump/main.go#L16) ignores
`filepath.WalkDir`'s return value, which trips `errcheck` and breaks
`make lint`. Existed before M4.8 (introduced in commit `8e97724`,
"M4.4–M4.7 close"); surfaced during M4.8 verification.

## Scope

Either check the error or assign to `_` like other call-sites in the repo
do. One-line fix; no test churn expected.

## Verification

- `make lint` passes.
- `go run ./cmd/diagdump` still produces the corpus diagnostic counts.
