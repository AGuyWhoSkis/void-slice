# M4.7 · `.decl.xml` sidecar XML handler

**Status:** todo
**Goal:** M4
**Size:** small

## What

`.decl.xml` files are Void Explorer export/import metadata: 12 corpus files, each a single self-closing XML element `<ResourceInfo name="…" classname="…" … />`. Per the taxonomy doc, the lint contract is "validate the XML well-formedness and emit nothing else." Today they route through the text scanner, producing 120 `PARSE_UNEXPECTED_TOKEN` + 36 `VOID_SCAN` (the `< / >` bytes). M4.3 wires extension-based dispatch for `.decl.xml`; this ticket fills in the handler behind it.

This is the **parser-layer sidecar golden coverage** ticket.

## Scope

- Implementation of the `.decl.xml` handler invoked by M4.3's dispatch on `.decl.xml` extension.
- Well-formedness check: parses successfully → zero diagnostics; malformed XML → a single targeted diagnostic (code chosen at PLAN time; document the choice in M4.2's allowlist if it is expected to fire on any corpus file — none should).
- The handler bypasses the text scanner entirely for `.decl.xml`, draining the residual VOID_SCAN sidecar count.

## Dependencies

M4.3 (extension-based dispatch route for `.decl.xml`).

## Verification

- Parse-package handler tests: well-formed sidecar emits zero diagnostics; a malformed-XML negative fixture emits exactly the targeted diagnostic. **Parser-layer sidecar golden coverage** on the representative [generated.decls.animbasic.…additives_body..animbasic.decl.xml](../../testdata/golden/generated.decls.animbasic.models.characters.dlc01.player.billie.additives_body..animbasic.decl.xml).
- M4.2's harness sidecar PARSE row (120) and VOID_SCAN row (36) drain to zero.
- `go test ./...` green.
