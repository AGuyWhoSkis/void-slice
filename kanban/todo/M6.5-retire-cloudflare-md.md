# M6.5 · Retire `cloudflare.md`, open M7

**Status:** todo
**Goal:** M6
**Size:** small
**Blocks:** nothing (final M6 ticket)

## What

`cloudflare.md` predates M6 and is now superseded:
- § 1 (architecture / numbers) is redundant with [architecture.md](../../architecture.md), the code, and the M2.7 / M2.8 retrospectives — drop.
- § 2 (deploy runbook) is replaced by M6's CI — drop.
- § 3 (deferred items / "missed gaps") is the only durable content — distil into a tickets table on a new "Production hardening" goal at [kanban/goals/M7.md](../goals/M7.md).

Also reconcile [kanban/goals/M6.md](../goals/M6.md) wording with the implementation (versions on a single script, not named environments) and the 5-ticket split.

## Scope

**Create [kanban/goals/M7.md](../goals/M7.md)** (status: `planned`). Tickets table covers the deferred items from `cloudflare.md` § 3:
- Diagnostic-count cap (1,000-emit hard stop in `parse.WalkEntities`'s diag-emit path → emit one final `LINT_DIAGNOSTIC_LIMIT` warning and stop).
- Post-deploy smoke from CI (`curl /health` + a known-broken `/lint` after each prod deploy).
- `web/public/_headers` (CSP, `X-Content-Type-Options`, `Referrer-Policy`).
- Cache layer on `(filename, sha256(src))` via the Cache API.
- `compatibility_date` discipline (intentional bumps, not drift).
- Automated rollback (`wrangler rollback` wired into the workflow).
- Custom-domain lockdown (`ALLOWED_ORIGIN` from `*` to the Pages domain).
- Workers Logs export to an external sink, when post-mortem visibility past 24h matters.

Frame the goal narrative: M6 made deploys *safe*, M7 makes them *robust*.

**Update [kanban/README.md](../README.md)**:
- Add an `M7 — Production hardening` row to the Goals table.

**Amend [kanban/goals/M6.md](../goals/M6.md)**:
- Replace "parametric `[env.preview]` alongside `[env.production]`" with wording that reflects the actual implementation: single-script Wrangler versions with per-PR preview aliases.
- Update the tickets table to list the 5 tickets that actually exist (M6.1 → M6.5), not the original 4.

**Delete `cloudflare.md`:** `git rm cloudflare.md`.

## Dependencies

None hard. Best done after M6.4 lands so the new branch-protection rules are accurately reflected in the M6.md amendment.

## Verification

- `cloudflare.md` is gone from the working tree.
- `kanban/goals/M7.md` exists at status `planned` with the 8 deferred items as a tickets table.
- `kanban/README.md` Goals table includes M7.
- `kanban/goals/M6.md` mentions versions/aliases (not `[env.preview]`) and lists 5 tickets.
- `grep -rn 'cloudflare\.md' .` returns no broken references (e.g. from `architecture.md` or `wrangler.toml` comments).
- `go test ./...` and `go vet ./...` pass.
