# M17.6 · SC-2 orphan-quote path investigation (read-only)

**Status:** todo
**Goal:** M17
**Size:** small
**Origin:** [M17-root-causes.md §3 ticket 4](../goals/M17-root-causes.md)

## What

[M17-root-causes.md §1's SC-2 verdict](../goals/M17-root-causes.md) is `needs-more-probing`. The cluster shape is bounded (3-9 codes across 38 repros) — not a UX emergency — but the residual after M12.3's existing unterminated-quote suppression at [`internal/parse/parse.go:317-331`](../../internal/parse/parse.go#L317-L331) and [`internal/parse/parse.go:689-705`](../../internal/parse/parse.go#L689-L705) is not pinned by code-reading alone. Specifically:

- One `VOID_SCAN` is correct (the rejected `'` byte at [`internal/scan/scan.go:187`](../../internal/scan/scan.go#L187)).
- A *second* `VOID_SCAN` comes from somewhere inside the scanner's orphan-quote forward-scan path at [`internal/scan/scan.go:80-108`](../../internal/scan/scan.go#L80-L108). It could be a separate unterminated-at-EOF emission, or a downstream byte the orphan run swallows then rejects.
- One to seven `PARSE_UNEXPECTED_TOKEN`s come from assignments structurally swallowed into the new orphan literal.

Investigate, then commit a memo at [`kanban/goals/M17-sc2-orphan-quote.md`](../goals/M17-sc2-orphan-quote.md) that resolves SC-2 to **exactly one** of three outcomes:

1. **Cascade-shaped, fixable (extends M12.3).** Identify the parser-side extension to `tokenIsUnterminatedQuote`/the M12.3 suppression site that closes the residual. Write a follow-up ticket (M17.6.1, or a fresh M17.* number per `/crud-ticket`'s rule at slice-time) describing the fix.
2. **Proportional to real structural damage (B2 inherent limit).** Map SC-2 to the **P-ProportionalToStructuralReach** principle named tentatively in [M17-root-causes.md §2](../goals/M17-root-causes.md). M17.7 picks up the doc surface.
3. **Classifier-router carve-out (M12.18 precedent).** Short-circuit the orphan-quote path at a higher level in [`internal/lint/lint.go`](../../internal/lint/lint.go)'s `classifyFile`. Write the follow-up ticket as for outcome 1.

This ticket is **read-only on engine code.** Output is a memo (always) and possibly a new ticket file (outcomes 1 and 3). No edits to `internal/scan/`, `internal/parse/`, `internal/validate/`, `internal/lint/`, `internal/report/`.

## Scope

- **Trace the 38 SC-2 repros end-to-end.** Source: the 38 `note=dquote-to-squote` rows in [`testdata/m17-probes/findings/punctuation.txt`](../../testdata/m17-probes/findings/punctuation.txt). For at least three repros spanning the magnitude range (one with the minimal 3-code cluster, one mid-range, one with the maximal 9-code cluster), do the trace by hand:
  - Locate the byte-flip offset in the source.
  - Predict where the scanner emits each `VOID_SCAN` by reading [`internal/scan/scan.go:80-108`](../../internal/scan/scan.go#L80-L108) (the `'"'` case) and [`internal/scan/scan.go:187`](../../internal/scan/scan.go#L187) (the `default` unknown-byte case).
  - Predict where the parser emits each `PARSE_UNEXPECTED_TOKEN` and the lone `PARSE_EXPECTED_SEMICOLON`, checking interaction with M12.3's suppression at [parse.go:317-331](../../internal/parse/parse.go#L317-L331) and [parse.go:689-705](../../internal/parse/parse.go#L689-L705).
  - Confirm the prediction by running `voidslice lint --json <variant>` locally on each repro variant (regenerate the variant with `go run ./testdata/m17-probes/run -family=punctuation` or by manual byte-flip) and comparing.

- **Optional probe.** If hand-tracing isn't conclusive, add a debug-print probe under [`testdata/m17-probes/run/`](../../testdata/m17-probes/run/) (a new `-family=sc2trace` file in the existing package, mirroring the shape of [`run/punctuation.go`](../../testdata/m17-probes/run/punctuation.go)) that walks the 38 SC-2 candidate files and dumps, for each, the byte offsets of every diagnostic in the cluster. This is exploratory tooling, not committed as a permanent harness — but the trace output goes into the memo's evidence section.

- **Write the memo at [`kanban/goals/M17-sc2-orphan-quote.md`](../goals/M17-sc2-orphan-quote.md).** Match the structure of [M17-root-causes.md](../goals/M17-root-causes.md): TL;DR with the verdict, §1 mechanism with line-anchored code citations, §2 verdict (one of the three outcomes named above), §3 if outcome 1 or 3, the follow-up ticket shape.

- **If outcome 1 or 3: write the follow-up ticket** under `kanban/todo/M17.<n>-<slug>.md` where `<n>` is the next free number per `/crud-ticket`'s rule at the time of writing. Numbering is computed at memo-close time, after this ticket's number is settled and after any other tickets that may have landed in the interim.

## Dependencies

None. Independent of M17.3, M17.4, M17.5. M17.7 depends on this ticket's outcome — until SC-2 resolves to a principle name (or is folded into a fix), M17.7's "plus whatever SC-2 resolves to" copy can't be written.

## Verification

- The memo file [`kanban/goals/M17-sc2-orphan-quote.md`](../goals/M17-sc2-orphan-quote.md) exists on the goal branch.
- The memo's verdict names exactly one of {cascade-shaped fixable, B2 inherent limit (P-ProportionalToStructuralReach), classifier-router carve-out}.
- The memo's §1 mechanism cites concrete line numbers in [`internal/scan/scan.go`](../../internal/scan/scan.go) and [`internal/parse/parse.go`](../../internal/parse/parse.go) — no hand-waving.
- The memo's §1 evidence section names at least three SC-2 repros walked end-to-end, with their predicted-vs-observed diagnostic offsets.
- If outcome 1 or 3: a follow-up ticket file exists at `kanban/todo/M17.<n>-<slug>.md`.
- `go test ./...` clean (no engine code was edited, but the optional probe if added must build).
- `git diff --stat` against the goal branch HEAD shows zero changes under `internal/scan/`, `internal/parse/`, `internal/validate/`, `internal/lint/`, `internal/report/`. Memo + optional probe only.

## Out of scope (intentionally deferred)

- Shipping the fix itself. If the memo lands outcome 1 or 3, the fix is a separate ticket — kept separate so the investigation can be reviewed independently of the implementation.
- Surfacing the verdict in user-facing docs — M17.7's job. M17.7 reads this ticket's memo and writes the playground/help copy from it.
- Probing the *other* surprise classes (SC-1, SC-3, SC-4). SC-1 and SC-3 are M17.3's territory; SC-4 is the durable B2 limit named in §2 already.
