# M10.6 · Canonical first nit: session-URL CLAUDE.md rule

**Status:** todo
**Goal:** M10
**Size:** small

## What
File the first real nit as the system's end-to-end smoke test. The captured rule: agents must never include `claude.ai/code/session_*` URLs in commit messages or PR bodies (Claude Code's git templates embed these by default; even though the URLs are auth-gated, the permanence of git history plus possible future access-model changes plus metadata leakage make this worth opting out of repo-wide). Adoption — the rule actually landing as a written line in `CLAUDE.md` — happens via a future goal cycle; this ticket only validates capture.

## Scope
- Run: `/nit "never include claude.ai/code/session_* URLs in commit messages or PR bodies"`
- When prompted for `Paths:`, answer: `CLAUDE.md`
- When prompted for context, answer:
  > Claude Code's git-commit and gh-pr-create templates embed `claude.ai/code/session_<id>` URLs by default. Even though the URLs are auth-gated, the permanence of git history plus possible future access-model changes plus metadata leakage make this worth opting out of repo-wide. Fix is a one-line CLAUDE.md rule.

## Dependencies
M10.1, M10.2.

## Verification
- `git fetch origin nits && git ls-tree -r origin/nits kanban/nits/ | grep session` returns exactly one file matching the slug pattern (`*-never-include-claude-ai-code-session*` or similar after slug truncation).
- `git show origin/nits:<that-file>` shows a `Paths:` block containing `CLAUDE.md`.
- `git show origin/nits:<that-file>` shows the context paragraph from Scope.

## Out of scope (intentionally deferred)
- Adopting the nit and writing the actual `CLAUDE.md` rule — that's a future goal's `/goal-slice`. The whole point is to test the surface-and-adopt flow with a real input; M10 ships the mechanism, the next goal exercises it.
- Filing additional nits to seed the system — one canonical nit is enough for the smoke test. More nits self-emerge from real usage.
