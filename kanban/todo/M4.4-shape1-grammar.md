# M4.4 · Shape 1 grammar drains corpus (curly inherit/edit)

**Status:** todo
**Goal:** M4
**Size:** medium

## What

Shape 1 is the largest in-scope grammar — 44 of 57 `.decl` files in `testdata/golden/` — and produces 120,990 of today's `PARSE_UNEXPECTED_TOKEN` hits. The existing `WalkEntities` (now wired as the Shape-1 walker by M4.3) was built for `.entities` and doesn't cover real Shape-1 patterns: tuple values (`color = ( 1, 1, 1 );`), the typed-block + nested object combinations seen across `arktree`, `cpntplayer*`, `entitydef`, `fx`, `kiscule`, `mapinfo`, `midnightscene`, `particledt`, `physicsmaterial`, `rulehandler`, `localized.*.speechbarks`, `localized.*.speechscene`. This ticket refines the Shape-1 walker until M4.2's harness shows the Shape-1 row at zero.

This is the **parser-layer Shape-1 golden coverage** ticket.

## Scope

- Refine the Shape-1 walker so every Shape-1 file in `testdata/golden/` parses with zero diagnostics (modulo any genuine validate warnings, which are M4.8's problem).
- Confirm tuple values, the `inherit = "…";` + `edit = { … }` skeleton, and the various sub-type-specific quirks in the 44 Shape-1 files all produce clean event streams.
- No new diagnostic codes introduced; if a real grammar gap surfaces a new code, propose a follow-up ticket per `/crud-ticket` PROPOSE shape rather than expanding scope here.

## Dependencies

M4.3 (dispatch must route Shape-1 files to this walker).

## Verification

- New parse-package golden event-trace tests on at least one representative per Shape-1 sub-type (covering inherit-only, inherit+edit, typed-block, tuple-valued). **Parser-layer golden coverage.**
- M4.2's harness Shape-1 row drains to zero; allowlist re-baselined accordingly.
- `go test ./...` green; existing scanner spot-checks (`goldenFileNames`) unaffected.
