# M4.13 · Decide the fate of `VOID_SCAN_STRUCTURE`

**Status:** todo
**Goal:** M4
**Size:** xs
**Origin:** M4.9

## What

[internal/scan/scan_constants.go:54](../../internal/scan/scan_constants.go#L54) declares `VOID_SCAN_STRUCTURE` and [internal/lint/coverage_audit_test.go:29](../../internal/lint/coverage_audit_test.go#L29) lists it as a known code, but no call site in the repo emits it. M4.9 had to skip it from the report-layer golden coverage because there's no real diagnostic to render.

Pick one:

- **Retire.** Delete the constant, drop it from `coverage_audit_test.go`'s `knownCodes`, and remove the skip comment in [internal/report/report_test.go](../../internal/report/report_test.go) (`TestGolden_AllCodesCovered`).
- **Wire up.** Find a structural scan-layer error class that warrants its own code (vs. the catch-all `VOID_SCAN`), emit it from the relevant site(s), then add a report-layer golden in M4.9's pattern.

## Scope

Whichever option wins. Cross-package: `internal/scan/`, `internal/lint/`, `internal/report/`.

## Verification

- `go test ./...` green.
- `go vet ./...` clean.
- If retired: no `VOID_SCAN_STRUCTURE` references remain; coverage audit still lists every emitted code.
- If wired up: at least one fixture emits it; report golden pins pretty + JSON renders.
