# M4.13 · M4 close: drain allowlist + retro

**Status:** todo
**Goal:** M4
**Size:** small

## What

Final ticket. Confirm M4.2's harness drains to its documented terminal state — Shape 4 (renderprog) PARSE/VOID permanently allowlisted per the G-C carve-out, plus whatever final state M4.8 leaves the entities-validate-warns in. Close M4: delete the closed tickets via `git rm` (per the kanban convention; closure record lives in `git log`), and append a retro section to `kanban/goals/M4.md` only if a non-obvious decision deserves preservation.

## Scope

- Run the full test suite + `make harnesses` and confirm green with the harness in its terminal allowlist state.
- Confirm every layer's golden coverage from the M4 slicing matrix is green: scanner (M4.6), parser (M4.3–M4.7), validator (M4.8), report (M4.9), lint facade (M4.2), wasm/worker (M4.12).
- If any layer's golden coverage is missing or weak, propose a follow-up ticket via `/crud-ticket` PROPOSE shape; do not silently let it slide.
- If a non-obvious decision was made during M4 (e.g. a rule was retired, a scanner approach was chosen over a grammar approach), append a brief retro section to [kanban/goals/M4.md](../goals/M4.md). Otherwise leave the goal file alone.

## Dependencies

M4.2 through M4.12.

## Verification

- `go test ./...` green.
- `make harnesses` green.
- M4.2's harness has no un-justified allowlist entries.
- The M4 goal can be moved to "closed" (per the project's convention: `git rm` of all M4.* tickets in this folder, except this one which is the closure commit's subject).
