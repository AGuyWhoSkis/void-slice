# M12.1 · Cascade characterization

**Status:** todo
**Goal:** M12
**Size:** medium

## What

Investigation-only ticket. Commit a minimal repro of the unterminated-quote cascade observed in M11 and characterize cascade scope across the corpus by per-shape mutation experiments on `testdata/golden/`. Conclude with a stress-test of the property-based locality-fuzzing technique pick from [linter-hardening-prospects.md](../goals/linter-hardening-prospects.md) — confirm by default; diverge only if the evidence demands it.

Shallow signal is fine where it pins the claim — a single failing fixture per shape is enough to characterize. The deliverable is a memo at `kanban/goals/M12-cascade-investigation.md`, modeled on M11's `M11-idtech-reach.md` and M4's `M4-decl-taxonomy.md`.

No code is touched — `internal/scan/`, `internal/parse/`, `internal/validate/`, `internal/lint/` stay clean for the harness ticket and fix tickets that will follow at M12.1's close.

## Scope

- **New file:** `kanban/goals/M12-cascade-investigation.md` with this skeleton (filled by this ticket):

  ```markdown
  # M12 — Cascade investigation

  **TL;DR:** <one-sentence verdict on (a) cascade scope across the corpus and (b) technique-pick stress test>

  ## 1. Repro fixture

  <pointer to `testdata/cascades/<shape>/<file>`, the mutation it captures, and byte-level cascade evidence — e.g. "voidslice emits N diagnostics; M of them fall outside lines X–Y, the mutated span">

  ## 2. Cascade shapes characterized

  <table or list: per candidate shape (unterminated quote, unterminated block comment, unterminated brace, missing semicolon, …) per format (.entities / .decl / .entitydef) per game (D2 / DOTO), whether mutation experiments produced cascades, with repro evidence and counts>

  ## 3. Technique-pick stress test

  <paragraph confirming property-based locality fuzzing per the prospects-doc recommendation, OR naming a divergence with rationale grounded in §2's evidence>
  ```

- **New fixture(s) under `testdata/cascades/`:** at minimum, the on-hand unterminated-quote sample committed under a per-shape subdir like `testdata/cascades/unterminated-quote/` holding a minimal repro file (smaller is more durable as the corpus evolves). Add sibling subdirs (`testdata/cascades/unterminated-block-comment/`, `testdata/cascades/unterminated-brace/`, …) only for shapes that §2's mutation experiments show actually cascade — one fixture per cascading shape.

- **Per-shape characterization method (§2):** for each candidate shape, apply the mutation at a known span to a sampled set of `testdata/golden/` files (drawing from each format `.entities` / `.decl` / `.entitydef` per game D2 / DOTO). Record whether `voidslice` emits diagnostics outside the mutated span. Tabulate by shape × format × game. The mutation experiments are run by hand or via a throwaway script; nothing under `internal/` lands.

- **Technique-pick conclusion (§3):** confirm the prospects-doc recommendation (property-based locality fuzzing) by default. Diverge only if §2's evidence shows the technique would miss a cascade shape; in that case name the alternative and the reasoning.

- **No code modified.** Investigation-only — `internal/scan/`, `internal/parse/`, `internal/validate/`, `internal/lint/`, `cmd/voidslice/`, and `internal/report/` are not touched.

- **No modification to `kanban/goals/M12.md`.** Status flip is the user's call after subsequent tickets land.

## Dependencies

None.

## Verification

- `kanban/goals/M12-cascade-investigation.md` exists with H1 `# M12 — Cascade investigation`, a `**TL;DR:**` line, and exactly three H2 headings — `## 1. Repro fixture`, `## 2. Cascade shapes characterized`, `## 3. Technique-pick stress test` — in that order.
- §1 body is non-stub: names a fixture path under `testdata/cascades/` and quotes byte-level cascade evidence (diagnostic line numbers vs. the mutated span).
- §2 body is non-stub: tabulates ≥3 candidate shapes (e.g. unterminated quote, unterminated block comment, unterminated brace) with cascade verdicts and per-shape × format × game evidence drawn from `testdata/golden/`.
- §3 body is non-stub: ends with a single sentence either confirming property-based locality fuzzing as the technique pick or naming a divergence with rationale.
- At least one fixture committed under `testdata/cascades/`. Running `go run ./cmd/voidslice lint <fixture>` on each committed fixture emits ≥1 diagnostic outside the span the memo identifies as the mutated span (cascade reproducible end-to-end).
- `git show --name-only <M12.1-commit>` lists only files matching `kanban/goals/M12-cascade-investigation.md` and paths under `testdata/cascades/`. No `internal/`, `cmd/`, or unrelated kanban edits.

## Out of scope (intentionally deferred)

- **Building the soundness harness.** Filed as a separate ticket at M12.1's close, once §3's technique-pick conclusion is in.
- **Fixing any cascade shape.** Per-shape fix tickets filed at M12.1's close — count and shape determined by §2's findings.
- **Cross-corpus differential between D2 / DOTO / Deathloop.** Deferred per the goal's scope unless §2 surfaces cross-corpus cascades; M12.1 may flag them but does not act on them.
- **Choice of fuzzing library or mutation strategy specifics.** That's the harness ticket's contract, made sharper by §2's shape inventory and §3's confirmation.
- **Modification of `kanban/goals/M12.md`.** Status flip is the user's call after subsequent tickets land.
