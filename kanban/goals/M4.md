# M4 — Linter scope refinement

Decide which validation rules belong in the linter once we have real-world signal — false-positive classes, schema awareness, NULL-ref handling, array-rule hits on valid corpus, live re-lint UX. M1 built the engine and 13 diagnostic codes; M4 is where we revisit the rule set with usage data in hand instead of a priori.

**Scope:** validation policy and rule set, not engine plumbing. Likely involves `internal/validate/`, possibly a new `internal/schema/` package, and decisions about which rules graduate from "fires on the corpus" to "fires on user files in production."

**Status:** parked — refinement conversation must happen first. [M4.1](../todo/M4.1-linter-scope.md) holds 5 open questions; no implementation begins until those become concrete deliverables. Gated on M2.5 (Cloudflare deploy) so we have real-world feedback to reason about. [M4.2](../done/M4.2-null-ref.md) (`VALIDATE_NULL_REFERENCE`) closed without implementation — its decision was absorbed into M4.1 open question #3.

## Tickets

| Ticket | What | Size |
|--------|------|------|
| [M4.1](../todo/M4.1-linter-scope.md)   | Linter scope — parking ticket for 5 open questions (false positives, schema awareness, NULL-ref, array hits, live re-lint UX) | medium (provisional) |
| [M4.2](../done/M4.2-null-ref.md) ✗ | NULL-reference validation — closed without implementation; absorbed into M4.1 open question #3 | small |
