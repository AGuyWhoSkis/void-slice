# M1 — Linter engine + CLI

The reason void-slice exists. Tokenize, parse, and validate Dishonored 2 / DOTO text-archive files (`.entities`, `.decl`, `.entitydef`) and surface diagnostics from a single CLI binary. Success looks like: a modder can run `voidslice lint <file>` and get useful, actionable lint output without setting up an editor or a server.

**Scope:** `internal/scan/`, `internal/parse/`, `internal/validate/`, `internal/report/`, `internal/lint/`, `cmd/voidslice/`. Anything that runs the linter as a CLI process. Excludes the HTTP server (M2), the LSP server (M3), and rule-set policy decisions (M4).

**Status:** active — critical path complete (M1.1–M1.9 done); only [M1.10](../todo/M1.10-cli-polish.md) (CLI polish — ANSI color, multi-file args, `--strict`) remains.

## Tickets

| Ticket | What | Size |
|--------|------|------|
| [M1.1](../done/M1.1-scaffold.md) ✓ | Project scaffold — rename `cmd/`, create `internal/lint` stub | small |
| [M1.2](../done/M1.2-parser.md) ✓ | Parser — cursor helpers + `WalkEntities` | large |
| [M1.3](../done/M1.3-validator.md) ✓ | Validator — array count/index rules | large |
| [M1.4](../done/M1.4-report.md) ✓ | Report rendering — human-pretty + JSON | medium |
| [M1.5](../done/M1.5-lint-facade.md) ✓ | Lint facade — single engine API, file classification, binary sniff | medium |
| [M1.6](../done/M1.6-cli.md) ✓ | CLI — `voidslice lint <file> [--json]` | medium |
| [M1.7](../done/M1.7-testdata.md) ✓ | Testdata — broken fixtures + golden snapshots | small |
| [M1.8](../done/M1.8-integration-tests.md) ✓ | Integration tests — corpus sweep | small |
| [M1.9](../done/M1.9-scan-tests.md) ✓ | Scan package — TDD coverage expansion | large |
| [M1.10](../todo/M1.10-cli-polish.md)   | CLI polish — ANSI color, multi-file args, `--strict` | small |
