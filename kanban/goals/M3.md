# M3 — Editor integration (LSP)

Make void-slice usable as you type. The CLI (M1) and the playground (M2) both require a paste-or-run loop; this goal closes that loop by feeding the linter into the editor's diagnostics panel over the Language Server Protocol. VS Code is the reference client. Success looks like: open a `.decl` file in any LSP-capable editor, see lint squiggles inline, no manual invocation.

**Scope:** `internal/lsp/`, `voidslice-vscode/`, integration tests harnessing the LSP subprocess. Excludes hover, go-to-definition, completions — diagnostics-only is the deliberate floor for this goal.

**Status:** active — the JSON-RPC server, subprocess integration tests, and a buildable VS Code extension all shipped. [M3.4](../todo/M3.4-lsp-followups.md) (umbrella: editor-setup docs for Neovim/Helix/Zed, VS Code e2e smoke test, server stderr logging, release pipeline for binary + `.vsix`) remains.

## Tickets

| Ticket | What | Size |
|--------|------|------|
| [M3.1](../done/M3.1-lsp-server.md) ✓ | LSP server — JSON-RPC 2.0 over stdio, wraps `internal/lint` | large |
| [M3.2](../done/M3.2-lsp-integration-tests.md) ✓ | LSP integration tests — subprocess harness against the corpus | small |
| [M3.3](../done/M3.3-vscode-extension.md) ✓ | VS Code extension — `voidslice-vscode/` packaged | small |
| [M3.4](../todo/M3.4-lsp-followups.md)   | LSP follow-ups — editor docs, VS Code e2e, stderr logging, release pipeline | umbrella |
