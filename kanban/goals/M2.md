# M2 — Cloud playground

A browser-accessible playground that runs the linter on user-pasted or uploaded files. The point is to lower the install barrier from "clone the repo, install Go, run a CLI" to "open a URL, paste a file, see diagnostics." Success looks like: the playground is publicly reachable, lints typical game-file inputs in under a second, and survives basic abuse (rate limit, body size cap).

**Scope:** `internal/server/`, the React frontend, container image, CI/CD, Cloudflare Worker / Pages glue, deploy-time docs. Excludes the linter engine itself (M1) and any LSP work (M3).

**Status:** active — backend, frontend, container, CI, WASM compile spike, and resource profile have all landed. Production deploy ([M2.5](../todo/M2.5-deploy.md)) and docs polish ([M2.6](../todo/M2.6-docs.md)) remain. See `cloudflare.md` at the repo root for the M2.5 handover.

## Tickets

| Ticket | What | Size |
|--------|------|------|
| [M2.1](../done/M2.1-http-server.md) ✓ | HTTP server — `POST /lint`, CORS, rate limits, health check | medium |
| [M2.2](../done/M2.2-frontend.md) ✓ | Frontend playground — React + Vite + CodeMirror | large |
| [M2.3](../done/M2.3-docker.md) ✓ | Containerization — distroless Dockerfile (<20MB) + compose | small |
| [M2.4](../done/M2.4-cicd.md) ✓ | CI/CD — GitHub Actions + Wrangler config | medium |
| [M2.5](../todo/M2.5-deploy.md)   | Production deploy — Cloudflare Pages + Worker + Container, custom domain | small |
| [M2.6](../todo/M2.6-docs.md)   | Docs polish — README → landing page, architecture doc, LSP design note | small |
| [M2.7](../done/M2.7-wasm-compile-spike.md) ✓ | WASM compile spike — does the linter build for `wasip1/wasm`? | small |
| [M2.8](../done/M2.8-linter-resource-profile.md) ✓ | Linter resource profile — memory + wall-clock against the corpus | small |
| [M2.9](../done/M2.9-k3d.md) ✗ | k3d Kubernetes lab — closed without implementation; superseded by Cloudflare Workers/WASM | medium |
